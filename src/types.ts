export interface SvgProps {
    color: string;
}

export type ExperienceProps = {
    company: string;
    title: string;
    date: string;
    location: string;
    website: string;
    logo: string;
}